spring:
  application:
    name: licensing-service
  profiles:
    active: dev
  config:
    import: configserver:http://config-server:8071

management:
  endpoints:
    enabled-by-default: true

  endpoint:
    env:
      show-values: ALWAYS
